import { Menu } from '../../components/Profile/Menu/Menu';

export const Profile = () => {
  return <Menu />;
};
// <Menu /> - використовується в одному місці і не використовується в інших місцях, тому його можна включити в цей файл
